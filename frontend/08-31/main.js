// Exercise 1: Instructions: Write a JavaScript function called greet that takes a name as an argument and returns a greeting message. For example, if the name is "John", the function should return "Hello, John!".
function greet(name) {
    console.log('Hello ${name}!');
}
person = 'John'
console.log(greet);

// Exercise 2: Write a JavaScript function called multiply that takes two numbers as arguments and returns their product. Test the function with different inputs.
function multiply(number1, number2) {
    return number1*number2;

}
product = multiply(4,5);
console.log(product);

//Exercise 3: Create a JavaScript object called person with properties for name, age, and gender. Assign appropriate values to the properties and display the object on the console.

const person1 = {
    name2: "Jamie",
    age: 32,
    gender: 'Male'
};
console.log(person1.name2);
console.log(person1.age);
console.log(person1.gender);

//Exercise 4: Create a JavaScript object called car with properties for make, model, and year. Assign appropriate values to the properties and display the object on the console.
